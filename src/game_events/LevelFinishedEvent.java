package game_events;

/** In Flying: An event that is fired when a flying-level is finished.
 * It activates the levelFinished-overlay.
 */
public record LevelFinishedEvent() implements GeneralEvent {}
